# # Build workflow for web-agents
# # Builds the React frontend, installs Python deps with uv, archives, and publishes.

# name: Build

# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     env:
#       PYTHON_VERSION: '3.12'
#       NODE_VERSION: '22.x'

#     steps:
#       - uses: actions/checkout@v4

#       - uses: actions/setup-node@v4
#         with:
#           node-version: ${{ env.NODE_VERSION }}
#           cache: npm
#           cache-dependency-path: frontend/package-lock.json

#       - name: Build frontend
#         run: |
#           cd frontend
#           npm ci
#           npm run build

#       - uses: astral-sh/setup-uv@v4
#         with:
#           version: latest

#       - uses: actions/setup-python@v5
#         with:
#           python-version: ${{ env.PYTHON_VERSION }}

#       - name: Install Python dependencies
#         run: uv sync --frozen

#       - name: Archive application
#         run: |
#           zip -r ${{ github.workspace }}/web-agents.zip . \
#             -x '.git/*' 'node_modules/*' '.venv/*' '__pycache__/*'

#       - uses: actions/upload-artifact@v4
#         with:
#           name: drop
#           path: ${{ github.workspace }}/web-agents.zip
